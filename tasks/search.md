# Search

Once a conversation has a lot of messages I can never find anything again. It'd be great to be able
to search old messages.

What "search" actually means here — and how far you take it — is up to you.

The search box in the sidebar and an empty `GET /api/search` route are already wired up, so you
shouldn't need to touch the frontend — just make the endpoint actually search.
